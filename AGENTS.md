# Book Scout 운영 원칙

- 개인용 Tailscale 서비스이며 별도 공개 배포나 Tailscale Serve 설정 없이 기존 포트 바인딩을 유지한다.
- 페이지 재진입 시 캐시된 목록을 자동 재조회하지 않는다. 목록 새로고침, 도서 추가, 상태 확인 후에는 서버의 최신 데이터를 반영한다.
- 목록 캐시 키에는 버전을 붙인다. `Book` 형태가 바뀌면 `CACHE_VERSION`을 올려 옛 캐시를 자동 폐기한다.
- 리뷰건수 정렬은 100자평과 리뷰 건수의 합계 내림차순이다. 가격 낮은순은 알라딘 매장 중고 가격 기준이며 가격 미확인 도서는 뒤로 보낸다.
- 알라딘 매장 재고는 도서별 권수를 저장해 `재고 있음(N권)`으로 표시한다. 재고 권수 정렬은 적은순이며 알라딘 재고 있음 필터에서만 허용한다.
